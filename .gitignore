# OS / editor noise only — this repo holds ONLY generated, redaction-gated public artifacts.
# Never commit secrets, keys, .env files, or the local overseer DB here.

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editors
*.swp
*.swo
*~
.idea/
.vscode/

# Misc
Thumbs.db
*.log

# Build output (Cloudflare Pages rebuilds it; never commit)
dist/

# Tooling artifacts
node_modules/
.wrangler/
.DS_Store

# Defensive: never let an env/key file in even by accident
.env
.env.*
*.key
*.pem
